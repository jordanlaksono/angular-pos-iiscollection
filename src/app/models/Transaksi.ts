export class Transaksi {
  kode_transaksi_master: number;
  faktur_bp: string;
  seri_bg: string;
  no_faktur: string;
  tgl_transaksi_master: string;
  debit_transaksi_master: string;
  kredit_transaksi_master: string;
  status_transaksi_master: string;
  kebijakan_transaksi_master: string;
  keterangan_transaksi_master: string;
  kode_nama_keuangan: string;
  kode_pegawai: string;
  kode_salesman: string;
  lampiran: string;
  tgl_tutup_buku: string;
  tgl_filter_tutup_buku: string;
  status_beban: string;
}
